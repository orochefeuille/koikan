<form method="post" action="{{ path('task_delete', {'id': task.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ task.id) }}">
    <button 
        class="btn btn-danger bg-orange-dark text-white rounded mx-1"
        data-toggle="tooltip" data-placement="top" title="Supprimer la tâche">
        <i class="far fa-times-circle fa-1x"></i>
    </button>
</form>
