{% if app.user %}
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    {# <a class="navbar-brand" href="#">EasyBlog</a> #}
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav w-100 d-flex">
          <a class="nav-link active" href="{{ path('project_index') }}">Projets en cours <span class="sr-only">(current)</span></a>
          <a class="nav-link" href="{{ path('project_archived') }}">Projets archivés</a>
          <a class="nav-link ml-lg-auto" href="{{ path('app_logout') }}">Déconnexion</a>
      </div>
    </div>
</nav>
{% endif %}